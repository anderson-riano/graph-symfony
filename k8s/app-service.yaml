apiVersion: v1
kind: Service
metadata:
  name: orderflow-app
  namespace: orderflow
spec:
  type: ClusterIP
  selector:
    app: orderflow-app
  ports:
    - name: http
      port: 80
      targetPort: 8000
